<div class="d-flex w-100 justify-content-between">

  <h4>{{title}}</h4>
  <small>
    {{#if (isCurrentUser user.username username)}}
    You posted
    {{else}}
    Posted by <span class="fst-italic">{{user.first_name}} {{user.last_name}}</span>
    {{/if}}
    on {{formatDate createdAt}}</small>
</div>
<p class="mb-1">{{ shortenArticle post_body }}</p>
<span class="badge text-bg-warning rounded-pill">{{comment_count}} comments</span>
